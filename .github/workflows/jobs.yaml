name: call-at-15-minutes
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call CRONWOD Edge Function on Vercel
        run: |
          curl --request GET \
          --url '${{ secrets.ENDPOINT }}' \
